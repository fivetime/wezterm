[package]
name = "wezterm-version"
version = "0.1.0"
edition = "2021"
build = "build.rs"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
# Not the workspace's git2: that one has "https", which pulls a system
# OpenSSL into this build script (none on macOS without Homebrew's); the
# script only reads the local repository.
git2 = { version = "0.20", default-features = false }

[dependencies]
